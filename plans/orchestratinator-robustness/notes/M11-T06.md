# M11-T06: Check that Change 0 is intact and every frontmatter block quotes its values

## Change 0

```
grep -rlF "They do not govern git" plugins/orchestratinator | sort
```
```
plugins/orchestratinator/agents/milestone-reviewer.md
plugins/orchestratinator/agents/plan-reviewer.md
plugins/orchestratinator/agents/planner.md
plugins/orchestratinator/agents/reviewer.md
plugins/orchestratinator/agents/scout-heavy.md
plugins/orchestratinator/agents/scout.md
plugins/orchestratinator/agents/specialist.md
plugins/orchestratinator/agents/worker-heavy.md
plugins/orchestratinator/agents/worker-light.md
plugins/orchestratinator/agents/worker.md
plugins/orchestratinator/skills/plan/SKILL.md
plugins/orchestratinator/skills/run/SKILL.md
```
OK: exactly the 12 expected files, no missing or extra file.

```
grep -rlF "omitClaudeMd: true" plugins/orchestratinator | sort
```
```
plugins/orchestratinator/README.md
plugins/orchestratinator/agents/specialist.md
plugins/orchestratinator/agents/worker-heavy.md
plugins/orchestratinator/agents/worker-light.md
plugins/orchestratinator/agents/worker.md
```
OK, with one difference from the expected 4-file list: `README.md` also matches. Read at `README.md:115`, it is prose describing the mechanism ("Workers read project instructions as files (`omitClaudeMd: true`) instead of receiving them as system instructions."), not a frontmatter setting, so it doesn't change which agents set the field. The four agent frontmatter blocks are exactly the expected `specialist.md`, `worker-heavy.md`, `worker-light.md`, `worker.md`.

```
grep -rlF "If one is a symlink to the other, or they have identical content, read it once." plugins/orchestratinator | sort
```
```
plugins/orchestratinator/agents/milestone-reviewer.md
plugins/orchestratinator/agents/plan-reviewer.md
plugins/orchestratinator/agents/planner.md
plugins/orchestratinator/agents/reviewer.md
plugins/orchestratinator/agents/scout-heavy.md
plugins/orchestratinator/agents/scout.md
plugins/orchestratinator/agents/specialist.md
plugins/orchestratinator/agents/worker-heavy.md
plugins/orchestratinator/agents/worker-light.md
plugins/orchestratinator/agents/worker.md
plugins/orchestratinator/skills/plan/SKILL.md
plugins/orchestratinator/skills/run/SKILL.md
```
OK: identical to the Step 1 list of 12 files, no missing or extra file.

```
grep -hF "They do not govern git" plugins/orchestratinator/agents/scout.md
grep -hF "They do not govern git" plugins/orchestratinator/agents/milestone-reviewer.md
grep -hF "They do not govern git" plugins/orchestratinator/agents/plan-reviewer.md
```
```
Project instruction files (CLAUDE.md, AGENTS.md, CLAUDE.local.md, `.claude/rules/`, and any nested or linked copies, whatever they're called) govern coding conventions, style, and project knowledge. They do not govern git. Where they say anything about committing, pushing, branching, stashing, resetting, or rewriting history, this plugin's rules replace them for the length of this task. You never commit, push, or change branches.
```
(all three outputs are this same one line, at `agents/scout.md:16`, `agents/milestone-reviewer.md:39`, `agents/plan-reviewer.md:33`)
OK: all three outputs are character-for-character identical, and each contains "You never commit, push, or change branches."

```
git diff 7d43fa8 HEAD -- plugins/orchestratinator | grep -E "^-[^-]" | grep -E "They do not govern git|If one is a symlink to the other|You never commit, push, or change branches|The orchestrator commits your work|omitClaudeMd: true|PUSHED|stray"
```
```
-2. Report: where, the reason (GAP, STUCK, SCOPE, VERIFY, REVIEW, MERGE, STRAY, PUSHED, SETUP, VALIDATION), the one-line detail, what the user needs to decide or fix, and the path of every worktree left for inspection. For a GAP, quote the question exactly.
```
OK: exactly the one expected removed line (the old Stop-section reason list, before `VACUOUS` was added). Confirmed at `plugins/orchestratinator/skills/run/SKILL.md:283`, the current line reads `2. Report: where, the reason (GAP, STUCK, SCOPE, VERIFY, REVIEW, VACUOUS, MERGE, STRAY, PUSHED, SETUP, VALIDATION), the one-line detail, what the user needs to decide or fix, and the path of every worktree left for inspection. For a GAP or VACUOUS, quote the question exactly.` — it still lists `PUSHED` and `STRAY`, and now also lists `VACUOUS`.

```
grep -nF "Check for stray commits" plugins/orchestratinator/skills/run/SKILL.md
```
```
plugins/orchestratinator/skills/run/SKILL.md:165
```
OK: line 165 is item 2 of `### 3d. Serial wave` (heading at line 154, next heading `### 3e. Parallel wave` at line 182). `### 3e. Parallel wave` (182–209) doesn't repeat the grepped phrase verbatim, but its item 3 first bullet, at line 189, reads "First, in that worktree, apply the same stray-commit and branch check as in 3d, using BASE as the recorded HEAD and the task branch as the expected branch." — the stray-commit check is still present in both 3d and 3e.

## Frontmatter

- `skills/plan/SKILL.md` — OK: `name`, `description` (no `: ` or leading special char, unquoted), `disable-model-invocation: true`, `argument-hint` (quoted with `"`, starts with `<` and contains `: `), `model: opus`.
- `skills/run/SKILL.md` — OK: `name`, `description` (unquoted, no `: ` or leading special char), `disable-model-invocation: true`, `argument-hint` (quoted with `"`, starts with `<` and contains `: `), `model: opus`.
- `skills/status/SKILL.md` — OK: `name`, `description` (unquoted, no `: ` or leading special char), `disable-model-invocation: true`, `argument-hint` (quoted with `"`, starts with `<`), `model: haiku`.
- `agents/milestone-reviewer.md` — OK: `name`; `description` quoted with `"` (not required — no `: ` and doesn't start with a special char — but quoting it anyway doesn't break YAML); `model: opus`; `effort: high`; `maxTurns: 60`; `disallowedTools: Edit`.
- `agents/plan-reviewer.md` — OK: `name`; `description` quoted with `"`, and required (contains `: ` in "checking its tasks against the plan format: banned phrases"); `model: sonnet`; `effort: high`; `maxTurns: 40`; `disallowedTools: Edit`.
- `agents/planner.md` — OK: `name`; `description` (unquoted, no `: ` or leading special char); `model: opus`; `effort: high`; `maxTurns: 60`.
- `agents/reviewer.md` — OK: `name`; `description` (unquoted, no `: ` or leading special char); `model: sonnet`; `effort: high`; `maxTurns: 30`; `disallowedTools: Write, Edit` (comma, no colon).
- `agents/scout-heavy.md` — OK: `name`; `description` quoted with `"`, and required (contains `: ` in "tracing logic across many files: control flow"); `model: sonnet`; `effort: high`; `maxTurns: 60`; `disallowedTools: Edit`.
- `agents/scout.md` — OK: `name`; `description` quoted with `"`, and required (contains `: ` in "Orchestratinator planning: answers precise questions"); `model: sonnet`; `effort: medium`; `maxTurns: 40`; `disallowedTools: Edit`.
- `agents/specialist.md` — OK: `name`; `description` (unquoted, no `: ` or leading special char); `model: opus`; `effort: high`; `maxTurns: 80`; `omitClaudeMd: true`.
- `agents/worker-heavy.md` — OK: `name`; `description` (unquoted, no `: ` or leading special char); `model: sonnet`; `effort: high`; `maxTurns: 60`; `omitClaudeMd: true`.
- `agents/worker-light.md` — OK: `name`; `description` (unquoted, no `: ` or leading special char); `model: haiku`; `maxTurns: 20`; `omitClaudeMd: true` (no `effort` line).
- `agents/worker.md` — OK: `name`; `description` (unquoted, no `: ` or leading special char); `model: sonnet`; `effort: medium`; `maxTurns: 40`; `omitClaudeMd: true`.

## Problems

None.
