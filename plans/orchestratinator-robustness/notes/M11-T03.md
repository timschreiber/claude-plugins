# M11-T03: Read-through of the Fails first, Origin, Batch, tier calibration, and Assumptions readers

## Fails first and RED

- `plugins/orchestratinator/reference/plan-format.md:229` — Fails first row: format 2 only, directly after Verify, `investigate` tasks omit it, `yes`/`no (<reason>)` forms, `review`-alone Verify is always `no`, format 1 has no field and is handled as `no` — OK
- `plugins/orchestratinator/reference/plan-format.md:156` — task template shows `- Fails first: yes` directly after `- Verify:` — OK
- `plugins/orchestratinator/reference/plan-format.md:241` — `- Blocked:` reasons list includes `VACUOUS` — OK
- `plugins/orchestratinator/reference/plan-format.md:321` — Validation checklist item: every `change` task has a Fails first line, no `investigate` task has one *(format 2)* — OK
- `plugins/orchestratinator/reference/plan-format.md:322` — Validation checklist item: test-adding tasks are `yes` with test-first-then-implementation Step order; `review`-alone tasks are `no` *(format 2)* — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:107` and `plugins/orchestratinator/agents/planner.md:59` — mention "under its Fails first rules" only in the Review Focus test-writing context — PROBLEM: neither `skills/plan/SKILL.md` step 6 ("Write the tasks as prompts") nor `agents/planner.md` "Write the tasks as prompts" has a passage instructing the author to set every task's Fails first line to `yes` (for a task that adds or changes tests) or `no (<reason>)` (otherwise), independent of Review Focus. The only place the field is mentioned in either is the Review Focus tie-in, which covers only tasks that own a Review Focus test, not test-adding tasks in general.
- `plugins/orchestratinator/agents/worker.md:45` (and identically `worker-light.md:44`, `worker-heavy.md:45`, `specialist.md:45`) — for `Fails first: yes`, test-writing Steps first, run Verify, confirm it fails before implementation, report `RED: CONFIRMED <first failing line>`; Verify passing early stops with `BLOCKED` / `GAP` and `RED: PASSED-EARLY` — OK
- `plugins/orchestratinator/agents/worker.md:46` (and identically `worker-light.md:45`, `worker-heavy.md:46`, `specialist.md:46`) — `RED: N/A` for `Fails first: no`, no Fails first line, or stopping before Verify ran (D31) — OK
- `plugins/orchestratinator/agents/worker.md:56-61`, `worker-light.md:55-60`, `worker-heavy.md:56-61`, `specialist.md:57-62` — Report block: `STATUS` / `REASON` / `FILES` / `VERIFY` / `RED` / `NOTE`, identical text in all four files, matching spec §12 character for character — OK
- `plugins/orchestratinator/skills/run/SKILL.md:166-168` — 3d: RED checked only for `- Fails first: yes`, before STATUS; `RED: PASSED-EARLY` → Block with GAP / `VACUOUS`; `DONE` with RED missing or `N/A` → Retry with reason `RED not confirmed (Fails first: yes)` — OK
- `plugins/orchestratinator/skills/run/SKILL.md:194-195` — 3e: same handling in the parallel wave path — OK
- `plugins/orchestratinator/skills/run/SKILL.md:265,269` — **Block with GAP** section: `RED: PASSED-EARLY` gets `VACUOUS` handling, never retried or escalated, `- Blocked: VACUOUS — <worker's NOTE>`, Open question `Verify passed before implementation: <worker's NOTE>`, then Stop as for GAP — OK
- `plugins/orchestratinator/skills/run/SKILL.md:283` — Stop section's reason list includes `VACUOUS`, alongside `PUSHED` — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:45` — check 7 "Fails first": every `change` task has the line directly after Verify, `investigate` tasks don't, test-adding tasks are `yes` with correct Step order, `no` needs a one-line reason and genuinely has no test that could fail beforehand, `review`-alone Verify is `no` — OK
- `plugins/orchestratinator/README.md:87` — Fails first bullet: test-adding tasks marked `yes`, Steps order, RED reporting, `PASSED-EARLY` proves nothing so run stops `VACUOUS` instead of retrying, missing confirmation gets a retry, no-test tasks are `no` with a reason — OK

## Origin

- `plugins/orchestratinator/reference/plan-format.md:231` — Origin row: only on a fix task, directly after Commit, no other task has it, a milestone with any `- Origin: review` task has used its one fix round — OK
- `plugins/orchestratinator/reference/plan-format.md:157` (task template) — `- Commit:` immediately followed by `- Origin: review` — OK
- `plugins/orchestratinator/reference/plan-format.md:21` — directory tree lists `notes/<milestone-id>-review.md`, `<milestone-id>-review-2.md`, and `<milestone-id>-plan-review.md` — OK
- `plugins/orchestratinator/agents/planner.md:84` — Fix findings mode step 4: fix tasks get `- Origin: review`, directly after `- Commit:` — OK
- `plugins/orchestratinator/agents/planner.md:85` — fix tasks follow the milestone's own Format (D10c) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:213` — 3f item 2: a milestone with any `- Origin: review` task goes straight to item 5 (the re-review) on resume — OK
- `plugins/orchestratinator/agents/milestone-reviewer.md:53` — re-review (`Re-review: fixes only`) reads `notes/<ID>-review.md`, checks blocking findings are fixed, and checks fix tasks (identified by `- Origin: review`) introduce no new blocking problem, reading each via its commit — matches D05 and D10 — OK

## Batch

- `plugins/orchestratinator/reference/plan-format.md:222` — Batch row: format 2 only, directly after Tier, optional (`- Batch: yes` or no line at all, never `no`), same-kind-no-logic edits, relaxes to about ten files and one Step per file, one commit, usually `worker-light`, waves still apply — OK
- `plugins/orchestratinator/reference/plan-format.md:235` — Steps row: "at most about seven steps, except in a batch task, which has one Step per file in its Files" — OK
- `plugins/orchestratinator/reference/plan-format.md:290-291` — Sizing rules 2 and 3 carry the batch exception (about ten files; one Step per file) — OK
- `plugins/orchestratinator/reference/plan-format.md:263-264` — "When in doubt, put the tasks in different waves." bullet immediately followed by the "Waves still apply to a batch task" bullet — OK
- `plugins/orchestratinator/reference/plan-format.md:324` — Validation checklist: batch Step-per-file check is the last item, directly after the Review Focus item (`:323`), and is marked *(format 2)* — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:102` — preference for one batch task over several tiny same-shape tasks, with the batching rules — OK
- `plugins/orchestratinator/agents/planner.md:49` — identical preference and rules — OK
- `plugins/orchestratinator/agents/reviewer.md:32` — for `- Batch: yes`, checks file by file that every listed file has its edit; a listed file with no change is a failure — OK
- `plugins/orchestratinator/agents/milestone-reviewer.md:51` — same check, "a listed file with no change is a blocking finding" (D38) — OK
- `plugins/orchestratinator/README.md:88` — Batching bullet, directly after the Fails first bullet (`:87`) — OK

## Tier calibration

- `plugins/orchestratinator/reference/plan-format.md:274` — `worker-light` row is spec §9's first bullet verbatim (literal final content, transcription plus verification, floor is `worker` if any step needs composing code from prose) — OK
- `plugins/orchestratinator/reference/plan-format.md:279` — rationale sentence ("cheapest models often take two to three times as many turns...") directly under the table — OK
- `plugins/orchestratinator/reference/plan-format.md:281` — `specialist` note ("more than about one task in ten") directly after the rationale sentence — OK
- `plugins/orchestratinator/reference/plan-format.md:283` — Tier adjustment note directly after the `specialist` note — OK
- `plugins/orchestratinator/agents/planner.md:22` — "Before anything else" item 8: reads the `- Escalated:` lines in every `done` milestone of the plan, not only the ones this milestone depends on, plus each escalated task's title and Objective — OK
- `plugins/orchestratinator/agents/planner.md:51` — escalation feedback: judges kind of task from title/Objective/reason; two or more escalations of the same tier on the same kind raises that kind one tier, on the ladder `worker-light` → `worker` → `worker-heavy` → `specialist`; writes `- Tier adjustment: <kind of task> → <tier> (<tier> escalated <n> times in <milestone IDs>)`; a raised `worker-heavy`/`specialist` task gives the adjustment as its Why this tier line — OK
- `plugins/orchestratinator/agents/worker-light.md:2` — frontmatter description: "Executes one Orchestratinator task whose Steps contain the literal final content to write (complete lines of code or config, exact file text)." — no mention of "boilerplate copied from a named file" — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:46` — check 8 "Tier fit": tests each task's Tier against the rubric "including the notes under its table" — OK

## Assumptions

- `plugins/orchestratinator/reference/plan-format.md:93-105` — `#### Assumptions` heading directly after the Open questions paragraph (`:91`) of `### Decisions and open questions`; Definition paragraph and five bullets (`:95-101`), Not assumptions sentence (`:103`), and the `[assumption]` tag sentence (`:105`), verbatim with spec §11 (D49) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:71` — step 5 says "four kinds of problem" — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:72-76` — **Assumption** bullet (`:76`) is the fourth, directly after the **Contradiction** bullet (`:75`) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:83` — "For each question" list includes the assumption line: "For an assumption, state what you would assume, why the plan needs it, and your recommended value, plus the evidence for a factual conclusion." — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:183` (step 11, "Write and hand off") — assumption check paragraph directly before the "Reply to the user with only:" line (`:185`); the reply bullet list ends with `` `Assumptions: none` `` — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:76` — points to "the Assumptions subsection of the plan format's 'Decisions and open questions' section" instead of restating it — OK
- `plugins/orchestratinator/agents/planner.md:29-33` — "Find every problem" has four categories (Insufficient, Ambiguous, Contradictory, An assumption at `:33`); an unsourceable assumption is a GAP tagged `[assumption]`, pointing to the plan format's Assumptions subsection instead of restating it — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:47` — check 9 "Unsourced assumptions": any value or choice no source, Decision, or cited fact supports is an issue, each reported separately, defined by the plan format's Assumptions subsection — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:48` — check 10 "Read first" follows check 9, matching D52's ordering — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:3` — frontmatter description names "unsourced assumptions" among the checks — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:52` — Calibration paragraph names "rest on an unsourced assumption" as one kind of issue — OK
- `plugins/orchestratinator/README.md:18-22` (item 1 under "How to use") — audits sources for "missing information, ambiguity, contradictions, and assumptions" — OK
- `plugins/orchestratinator/README.md:82` — "Questions answered first" bullet: names the four categories, defines an assumption, says what its question contains, that `plan` checks the finished plan before handoff so it says `Assumptions: none`, and that the planner and plan-reviewer catch unsourced ones — OK

## Problems

1. `plugins/orchestratinator/skills/plan/SKILL.md:96-107` (step 6, "Write the tasks as prompts") and `plugins/orchestratinator/agents/planner.md:47-53` ("Write the tasks as prompts") — neither passage instructs the author to set each task's Fails first line (`yes` for a task that adds or changes tests, `no (<reason>)` otherwise); the only Fails first mention in either is the Review Focus tie-in, which applies only to a task that owns a Review Focus test, not to test-adding tasks generally (Fails first and RED; spec §5, plan-format.md:229, D08)
