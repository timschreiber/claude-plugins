# M11-T07: Read-through result

## Sources

- `plans/orchestratinator-robustness/notes/M11-T02.md` — 0 problems
- `plans/orchestratinator-robustness/notes/M11-T03.md` — 1 problem
- `plans/orchestratinator-robustness/notes/M11-T04.md` — 0 problems
- `plans/orchestratinator-robustness/notes/M11-T05.md` — 1 problem
- `plans/orchestratinator-robustness/notes/M11-T06.md` — 0 problems

## Problems

1. `plugins/orchestratinator/skills/plan/SKILL.md:96-107` (step 6, "Write the tasks as prompts") and `plugins/orchestratinator/agents/planner.md:47-53` ("Write the tasks as prompts") — neither passage instructs the author to set each task's Fails first line (`yes` for a task that adds or changes tests, `no (<reason>)` otherwise); the only Fails first mention in either is the Review Focus tie-in, which applies only to a task that owns a Review Focus test, not to test-adding tasks generally (Fails first and RED; spec §5, plan-format.md:229, D08) [from M11-T03]
2. `plugins/orchestratinator/skills/run/SKILL.md:256,267` — the parenthetical "(step 6 onward)" in both the **Retry** and **Block with GAP** sections doesn't resolve: `run/SKILL.md` has no `## 6.` section (its top-level numbering is only `## 1.` to `## 4.`), so there is no target named "step 6" anywhere in the file. The surrounding text ("finish the wave's other tasks first") most likely means `### 3e. Parallel wave`'s own item 6, "Integrate" (line 206), which is where a wave resumes after its other tasks' reports are in, but the reference as written doesn't name that or any other real target. (Step and item references; spec §16 read-through) [from M11-T05]
