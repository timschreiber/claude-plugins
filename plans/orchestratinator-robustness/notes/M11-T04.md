# M11-T04: Read-through of the two new agents' dispatch contracts

## milestone-reviewer

- `agents/milestone-reviewer.md:4-7` — frontmatter has `model: opus`, `effort: high`, `maxTurns: 60`, `disallowedTools: Edit` — OK
- `agents/milestone-reviewer.md:45-49` — numbered checks 1-5: Coverage, Interfaces, Review Focus, Rules (Decisions/Context/CLAUDE.md/AGENTS.md), Code quality, matching spec §7's checks 1-5 in order — OK
- `agents/milestone-reviewer.md:49` — check 5 runs each distinct test-running Verify command of the milestone's tasks once, from the repository root (D37) — OK
- `agents/milestone-reviewer.md:59-73` — report file has `## Blocking` and `## Advisory`, each finding one line ``- `path:line` — <problem> (<task ID, Coverage row, or D<nn>>)``, `None.` for an empty section, matching D21's line format — OK
- `agents/milestone-reviewer.md:53` — `Re-review: fixes only` reads `notes/<ID>-review.md`, checks only that each `## Blocking` finding is fixed and that the fix tasks (`- Origin: review`) introduce no new blocking problem, via `git log --grep` and `git show` — OK, matches D05
- `agents/milestone-reviewer.md:75-83` — reply block is `STATUS: APPROVED | FINDINGS`, `BLOCKING: <count>`, `ADVISORY: <count>`, matching spec §7's block — OK

## milestone-reviewer dispatch

First review, `skills/run/SKILL.md:213-219` sends:
```
Plan: <plan dir>
Milestone: <ID>
Base: <Base>
Output: <plan dir>/notes/<ID>-review.md
```
Re-review, `skills/run/SKILL.md:230-237` sends:
```
Plan: <plan dir>
Milestone: <ID>
Base: <Base>
Output: <plan dir>/notes/<ID>-review-2.md
Re-review: fixes only
```
`agents/milestone-reviewer.md:14-27` says it receives:
```
Plan: <plan dir>
Milestone: <ID>
Base: <sha of the milestone's "chore(plan): start <ID>" commit>
Output: <plan dir>/notes/<ID>-review.md
```
plus, for a re-review, `Output` becomes `<plan dir>/notes/<ID>-review-2.md` with `Re-review: fixes only` as one more line.

- Same keys in the same order (Plan, Milestone, Base, Output, [Re-review]) for both the first review and the re-review, and the Output path matches `-review.md` / `-review-2.md` as required — OK
- `skills/run/SKILL.md:213` — Base is found with `git log --format=%H --grep="^chore(plan): start <ID>$"`, taking the oldest match (D22) — OK
- `skills/run/SKILL.md:221,239` — the reply keys 3f reads (`STATUS`, `BLOCKING`) are keys of the agent's reply block (`STATUS`, `BLOCKING`, `ADVISORY`); 3f's text also references advisory findings not holding the milestone up (item 3), consistent with the block's `ADVISORY` key — OK
- `skills/run/SKILL.md:220,229,238` — commit messages `chore(plan): review <ID>`, `chore(plan): fix tasks <ID>`, `chore(plan): re-review <ID>` (D10d) — OK
- `skills/run/SKILL.md:220` — the report is committed as soon as the reviewer replies (or skipped if `git status --porcelain` prints nothing), after a scope check allowing only that file (D35) — OK
- `skills/run/SKILL.md:238-239` — a re-review still `BLOCKING` above 0 marks the milestone `blocked` before the `REVIEW` stop (D36) — OK
- `skills/run/SKILL.md:213` — a milestone with any `- Origin: review` task has used its one fix round; `run` goes straight to item 5 (re-review) on resume (D10a) — OK

## plan-reviewer

- `agents/plan-reviewer.md:4-7` — frontmatter has `model: sonnet`, `effort: high`, `maxTurns: 40`, `disallowedTools: Edit` — OK
- `agents/plan-reviewer.md:39-48` — checks 1 to 10 (banned phrases and placeholders, Sonnet test, Coverage, Interfaces, wave interference, Verify, Fails first, tier fit, unsourced assumptions, Read first) follow spec §8's list in order (D52) — OK
- `agents/plan-reviewer.md:37` — format 1 skip line names checks 3, 4, and 7 (D52) — OK
- `agents/plan-reviewer.md:3` — frontmatter description's list of checks names unsourced assumptions — OK
- `agents/plan-reviewer.md:52` — Calibration's definition of an issue names "rest on an unsourced assumption" — OK
- `agents/plan-reviewer.md:56-64` — report file has `## Issues` as a numbered list, `<task or milestone ID> — <which check> — <problem, quoting the text involved>`, `None.` with no issues, matching D21's format — OK
- `agents/plan-reviewer.md:66-73` — reply block is `STATUS: APPROVED | ISSUES`, `ISSUES: <count>`, matching spec §8's block — OK

## plan-reviewer dispatch

`skills/run/SKILL.md:113-118` sends:
```
Plan: <plan dir>
Milestone: <ID>
Output: <plan dir>/notes/<ID>-plan-review.md
```
`skills/plan/SKILL.md:175-179` sends:
```
Plan: <plan dir>
Milestone: <ID>
Output: <plan dir>/notes/<ID>-plan-review.md
```
`agents/plan-reviewer.md:14-20` says it receives:
```
Plan: <plan dir>
Milestone: <ID>
Output: <plan dir>/notes/<ID>-plan-review.md
```

- All three are `Plan: <plan dir>`, `Milestone: <ID>`, `Output: <plan dir>/notes/<ID>-plan-review.md`, same keys, same order (D09) — OK
- `skills/plan/SKILL.md:173,181` — `plan` dispatches all detailed milestones in one message (D09), does one fix pass with no re-review, revalidates every milestone it changed, and the handoff gives issue totals across all detailed milestones (`skills/plan/SKILL.md:190`, D41) — OK
- `skills/run/SKILL.md:130-131` — 3a's scope check allows `notes/<ID>-plan-review.md` and it is committed together with the detailed milestone in `chore(plan): detail <ID>`, with no commit of its own (D40) — OK
- `skills/run/SKILL.md:126` — a plan-review fix pass that reports `BLOCKED` / `GAP` discards the uncommitted detailed milestone file with `git checkout -- "<milestone file path>"`, restoring the committed outline, and handles the GAP as in item 4 (D42) — OK
- `skills/run/SKILL.md:222-229` (3f item 4, fix round) — dispatches `orchestratinator:planner` with `Fix findings:`, never `orchestratinator:plan-reviewer`; fix tasks from a milestone review are not sent to `plan-reviewer` (D10b) — OK

## Planner modes

`skills/run/SKILL.md:222-227` (3f item 4) sends the extra line:
```
Fix findings: <plan dir>/notes/<ID>-review.md
```
`agents/planner.md:79` (Fix findings mode) expects:
```
Fix findings: <path>
```
— matches spec §7's `Fix findings: <plan dir>/notes/<ID>-review.md` line — OK

`skills/run/SKILL.md:119-124` (3a item 5) sends the extra line:
```
Plan review: <plan dir>/notes/<ID>-plan-review.md
```
`agents/planner.md:91` (Plan review mode) expects:
```
Plan review: <path>
```
— matches spec §8's `Plan review: <path>` line — OK

- `agents/planner.md:79-87` — Fix findings mode fixes only findings under `## Blocking` (item 1), never reports `SCOUT` (item 1: "There is no scout round in this mode, so never report SCOUT"), and changes nothing in the milestone file except appending fix tasks and updating the Waves line (item 6), consistent with D34 — OK
- `agents/planner.md:89-98` — Plan review mode fixes every issue under `## Issues` (item 1), never reports `SCOUT` (item 1), and edits only the files detailing allows: the milestone file and plan.md's Decisions, Open questions, Coverage, and this milestone's table row (item 4), consistent with D39 — OK

## Listings

- `reference/plan-format.md:8-9` — the reader list at the top names both `plan-reviewer` and `milestone-reviewer` (D24) — OK
- `README.md:69-70` — the cast table has a row for `plan-reviewer` and `milestone-reviewer` — OK
- `README.md:103,112` — "How a run behaves" (a `###` heading) describes the milestone review: Coverage, Interfaces, Review Focus, Decisions/Context/CLAUDE.md/AGENTS.md, code quality, the fix round, the re-review, and the `REVIEW` stop — OK

## Problems

None.
