# M11-T02: Read-through of the Format, Interfaces, Coverage, and Review Focus readers

## Format

- `reference/plan-format.md:112-113` — milestone template has `- Status: outline` then `- Format: 2` directly after it — OK
- `reference/plan-format.md:188` — `### Format` prose: a milestone with `- Format: 2` directly after Status is format 2; without that line it is format 1; `plan` always writes format 2, detailed or outlined; the planner always writes format 2 when it details, even in a format 1 plan; validation applies the *(format 2)* checklist items only to format 2 milestones, so format 1 milestones validate as before — OK
- `reference/plan-format.md:314-324` — Validation checklist: Interfaces items (314-317) *(format 2)*, plan-level Coverage (318) carries its own condition with no *(format 2)* mark (matches D27), milestone Coverage and its task-ID item (319-320) *(format 2)*, Fails first items (321-322) *(format 2)*, Review Focus item (323) *(format 2)*, Batch item (324) *(format 2)* — OK
- `skills/plan/SKILL.md:171` — "Every milestone file, detailed or outlined, gets the line `- Format: 2` directly after its Status line." — OK
- `agents/planner.md:104` — Write section: "put `- Format: 2` directly after the Status line, adding it if it's missing, even in a plan created under format 1" — OK
- `agents/planner.md:69` — Build the Coverage section: "If this milestone's file has no `- Format: 2` line when you start, the plan was created under format 1 ... If plan.md has no `## Coverage` section, create it between `## Milestones` and `## Decisions`." — OK
- `skills/run/SKILL.md:136,171,213` — run invokes "the validation checklist" generically at 3b (136), which already carries the format-2-only marking from `reference/plan-format.md`; the only explicit "format 1 milestones" text is the Fails-first RED-check exemption at 171 ("as in format 1 milestones and `investigate` tasks"); 213 says "Every milestone is reviewed, whatever its format." run has no separate restatement of "format 1 milestones still validate and run" beyond invoking the checklist and citing plan-format.md, which is itself the authority for that guarantee (`reference/plan-format.md:188`) — OK
- `agents/plan-reviewer.md:37` — "A milestone file without a `- Format: 2` line is format 1: ... so skip checks 3, 4, and 7 for it and run the rest." — OK
- `agents/milestone-reviewer.md:43` — "A milestone file without a `- Format: 2` line is format 1: ... so skip checks 1 to 3 for it and run checks 4 and 5." Combined with `skills/run/SKILL.md:213`'s "reviewed, whatever its format", every milestone (format 1 included) is reviewed — OK
- `agents/reviewer.md:31` — "A task without an Interfaces block (a format 1 task) skips this check." — OK

## Interfaces

- `reference/plan-format.md:169-174` — task template's Interfaces block with two Consumes lines and two Produces lines — OK
- `reference/plan-format.md:234` — Task fields Interfaces row: "Format 2 milestones only"; at least one Consumes and one Produces, `none` allowed for either (D25); Consumes names a task ID or `existing` with a `path:line`; "A symbol produced by a format 1 task, which has no Produces, is cited as `existing` with a `path:line`" (D11) — OK
- `reference/plan-format.md:314-317` — Validation checklist: Interfaces block present with a Consumes and Produces line (314), exact signature/sourcing (315), Consumes matches Produces character for character + Depends on (316), no symbol produced by two tasks with different signatures (317) — OK
- `skills/plan/SKILL.md:127` — step 8 self-check: "Run an interface-consistency pass across all tasks of every milestone you detailed, including Consumes that cite a task in another milestone" — OK
- `agents/planner.md:75` — Self-check section: "Run an interface-consistency pass across all tasks in this milestone and against the Produces of `done` milestones" — OK
- `agents/planner.md:20` — "Before anything else" item 6: "For each of their tasks that has an Interfaces block, also read its Produces lines." (D26) — OK
- `agents/worker.md:43`, `agents/worker-light.md:42`, `agents/worker-heavy.md:43`, `agents/specialist.md:43` — "If the task has an Interfaces block: implement every Produces entry exactly as written, and never change the signature of anything consumed. If the code disagrees with a Consumes entry, stop and report `BLOCKED` / `GAP`." Text is word-for-word identical across all four files — OK
- `agents/reviewer.md:31` — "the code matches every Produces entry exactly as written (names, parameter and return types, constant values)" — OK
- `agents/plan-reviewer.md:42` — check 4, Interfaces — OK
- `agents/milestone-reviewer.md:46` — check 2, Interfaces — OK
- `README.md:84` — Interfaces bullet under "What a plan contains" — OK

## Coverage

- `reference/plan-format.md:44-57` (plan.md template: `## Milestones` at 44, `## Coverage` at 51, `## Decisions` at 57) and `:194` (prose: "plan.md, `## Coverage`, between Milestones and Decisions") — OK
- `reference/plan-format.md:119-133` (milestone template: Context at 119, its Waves line inside Context, `## Coverage` at 127, `## Review Focus` at 133) and `:195` (prose: "Milestone file, `## Coverage`, directly after Context and its Waves line *(format 2)*") — OK
- `reference/plan-format.md:199` — row format `- <source> <location>: <requirement, briefly> → <task IDs | milestone ID | out of scope (D<nn>)>`, and what makes a row mapped/unmapped — OK
- `reference/plan-format.md:318` — plan-level Coverage checklist item carries its own condition ("when every milestone in the plan is format 2 ... otherwise, every format 2 milestone has at least one row ... mapped to it"), with no *(format 2)* mark (matches D27) — OK
- `skills/plan/SKILL.md:131-140` — step 9 builds both coverage levels — OK
- `skills/plan/SKILL.md:151` — "The size check never skips the coverage check, but a job done directly writes no Coverage anywhere." (D18) — OK
- `skills/plan/SKILL.md:171` — "plan.md gets the Coverage section from step 9, between its Milestones and Decisions sections, and every detailed milestone file gets its own Coverage section, directly after its Context and Waves line." — OK
- `agents/planner.md:67-71` — "## Build the Coverage" section — OK
- `agents/plan-reviewer.md:41` — check 3, Coverage — OK
- `agents/milestone-reviewer.md:45` — check 1, Coverage — OK
- `README.md:85` — Coverage bullet — OK

## Review Focus

- `reference/plan-format.md:127,133` (milestone template: `## Coverage` at 127, `## Review Focus` at 133, directly after it) and `:205` (prose: "Every detailed milestone has a `## Review Focus` section, directly after its Coverage section *(format 2)*") — OK
- `reference/plan-format.md:137` (template line) and `:207` (prose) — ``- <input or condition> → <expected behavior> (source: <§ or D<nn>>). Test: `<test name>` in <task ID>.`` — OK
- `reference/plan-format.md:209` — "If nothing qualifies, the section is one line, `None found: <what was checked>`. The section is never blank." — OK
- `reference/plan-format.md:323` — Validation checklist item for Review Focus — OK
- `skills/plan/SKILL.md:107` — "Once a milestone's tasks are drafted, build its Review Focus ... before you sequence the tasks in step 7." (D32) — OK
- `skills/plan/SKILL.md:171` — "Every detailed milestone file also gets its Review Focus section from step 6, directly after its Coverage section." — OK
- `agents/planner.md:57` (`## Build the Review Focus`) directly before `agents/planner.md:63` (`## Sequence and find the parallelism`) — matches D32 — OK
- `agents/planner.md:61` — "If it doesn't, it's a design decision, not yours to make: report `BLOCKED` / `GAP` and write the question to Open questions, tagged `[ambiguous]`, as in 'Find every problem'." (D33) — OK
- `agents/milestone-reviewer.md:47` — check 3, Review Focus — OK
- `README.md:86` — Review Focus bullet, between the Coverage bullet (`README.md:85`) and the Fails first bullet (`README.md:87`), matching D33 — OK

## Problems

None.
