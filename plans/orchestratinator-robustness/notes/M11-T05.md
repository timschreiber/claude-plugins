# M11-T05: Check that every cross-reference in the plugin resolves

## Step and item references

- `plugins/orchestratinator/skills/plan/SKILL.md:41,67,109,150,181,183` — "step 5" refers to this file's `## 5. Find every problem and ask about it` (line 69) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:162,171` — "step 6" refers to this file's `## 6. Write the tasks as prompts` (line 95) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:102,107,162` — "step 7" refers to this file's `## 7. Sequence the tasks and find the parallelism` (line 111) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:138` — "steps 7 and 8" refers to `## 7.` (line 111) and `## 8. Self-check` (line 121) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:151,171` — "step 9" refers to this file's `## 9. Check spec coverage` (line 131) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:181,183` — "steps 6 to 9" refers to `## 6.` through `## 9.` (lines 95, 111, 121, 131) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:13,19,50,181` — "step 10" refers to this file's `## 10. Size check: do small jobs directly` (line 142) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:50,140,158` — "step 11" / "Step 11" refers to this file's `## 11. Write and hand off` (line 169) — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:161` — "item 1" refers to step 10's own item 1 (line 155, "Ask for approval first") — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:164` — "item 2" refers to step 10's own item 2 (line 161, "Note whether `git status --porcelain` is empty") — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:155` — "item 5" ("see item 5") refers to step 10's own item 5 (line 164, "Commit only if...") — OK
- `plugins/orchestratinator/skills/run/SKILL.md:18` — "(2b)" refers to this file's `### 2b. Ask for approval` (line 63) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:61` — "in 2c" refers to this file's `### 2c. Prepare` (line 82), which marks tasks `done` in its item 2 — OK
- `plugins/orchestratinator/skills/run/SKILL.md:85` — "noted in 2a" refers to this file's `### 2a. Checks` (line 52) item 6, which notes interrupted-run recovery tasks — OK
- `plugins/orchestratinator/skills/run/SKILL.md:111` — "exactly as in item 2" refers to 3a's own item 2 (line 106, "Plan.") — OK
- `plugins/orchestratinator/skills/run/SKILL.md:126` — "handle it as in item 4" refers to 3a's own item 4 (line 112, `BLOCKED` / `GAP` handling) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:149` — "(3d or 3e)" refers to `### 3d. Serial wave` (line 154) and `### 3e. Parallel wave` (line 182) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:193` — "as in 3d" refers to `### 3d. Serial wave` (line 154), whose item 2 is the stray-commit/branch check — OK
- `plugins/orchestratinator/skills/run/SKILL.md:213,229` — "item 5" (within 3f) refers to 3f's own item 5 (line 230, "Re-review") — OK
- `plugins/orchestratinator/skills/run/SKILL.md:221` — "go to item 7" refers to 3f's own item 7 (line 240, "Set the milestone to `done`") — OK
- `plugins/orchestratinator/skills/run/SKILL.md:228` — "handle it as in 3a item 4" refers to `### 3a` (line 95) item 4 (line 112, `BLOCKED` / `GAP` handling) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:229` — "wave loop (3c)" refers to `### 3c. Wave loop` (line 138) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:230` — "handling a failure as in item 1" refers to 3f's own item 1 (line 212, "Run the Milestone verify command") — OK
- `plugins/orchestratinator/skills/run/SKILL.md:238` — "as in item 2" refers to 3f's own item 2 (line 213, "Review", whose commit logic this parallels) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:256,267` — "(step 6 onward)" — PROBLEM: `skills/run/SKILL.md` has no `## 6.` section (its top-level sections are only `## 1.` to `## 4.`; line 89's `## 3. Milestone loop` holds subsections `3a`–`3f`). The surrounding text ("finish the wave's other tasks first") describes finishing the rest of a parallel wave's batch before integrating, which is `### 3e. Parallel wave`'s own item 6 ("Integrate", line 206), not a "step 6". No section or item numbered 6 that this could mean is named "step 6" anywhere in the file.

Headings that are themselves numbered-section declarations matching the grep pattern (`### 2a.` line 52, `### 2b.` line 63, `### 2c.` line 82, `### 3a.` line 95, `### 3b.` line 134, `### 3c.` line 138, `### 3d.` line 154, `### 3e.` line 182, `### 3f.` line 210) are definitions, not references to another target, so they are skipped and recorded as nothing, per the Steps 1 skip rule. `plugins/orchestratinator/agents/*.md`, `plugins/orchestratinator/README.md`, and `plugins/orchestratinator/reference/plan-format.md` have zero matches for the Step 1 grep (confirmed by running it against each individually).

## Section references and links

- `plugins/orchestratinator/skills/run/SKILL.md:164,168,173,177,178,195,197,198` (uses of `**Retry**`) — refers to this file's `## Retry` (line 252) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:167,172,194,196` (uses of `**Block with GAP**`) — refers to this file's `## Block with GAP` (line 265) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:132,152,241` (uses of `**Pause**`) — refers to this file's `## Pause` (line 271) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:105,112,129,130,136,140,165,175,186,199,205,208,212,220,229,239,248,256,267` (uses of `**Stop**`, some lines have it twice) — refers to this file's `## Stop` (line 278) — OK
- `plugins/orchestratinator/skills/run/SKILL.md:34,39` — `**Verify a command**` and `**Commit a task**` Definitions bullets; no other file or line refers to either by name — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:76` — `"Decisions and open questions"` refers to `reference/plan-format.md:87` (`### Decisions and open questions`) — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:47` — `"Decisions and open questions"` refers to `reference/plan-format.md:87` — OK
- `plugins/orchestratinator/agents/planner.md:33` — `"Decisions and open questions"` refers to `reference/plan-format.md:87` — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:97` — `"Tasks are prompts"` refers to `reference/plan-format.md:243` (`## Tasks are prompts`) — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:40` — `"Tasks are prompts"` refers to `reference/plan-format.md:243` — OK
- `plugins/orchestratinator/agents/planner.md:47` — `"Tasks are prompts"` refers to `reference/plan-format.md:243` — OK
- `plugins/orchestratinator/skills/plan/SKILL.md:117` — `"Sequence and parallelism"` refers to `reference/plan-format.md:252` (`## Sequence and parallelism`) — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:43` — `"Sequence and parallelism"` (the "five interference rules") refers to `reference/plan-format.md:252`, whose list (lines 257–262) has exactly 5 numbered rules — OK
- `plugins/orchestratinator/agents/planner.md:65` — `"Sequence and parallelism"` refers to `reference/plan-format.md:252` — OK
- `plugins/orchestratinator/agents/plan-reviewer.md:45` — `"Run Verify and confirm it fails"` refers to `reference/plan-format.md:229` (Fails first row, exact phrase) — OK
- `plugins/orchestratinator/agents/planner.md:9` — `"Fix findings mode"` refers to this file's `## Fix findings mode` (line 77) — OK
- `plugins/orchestratinator/agents/planner.md:9` — `"Plan review mode"` refers to this file's `## Plan review mode` (line 89) — OK
- `plugins/orchestratinator/agents/planner.md:81,93` — `"Before anything else"` refers to this file's `## Before anything else` (line 11) — OK
- `plugins/orchestratinator/agents/planner.md:71,83,95` — `"Find every problem"` refers to this file's `## Find every problem` (line 26) — OK
- `plugins/orchestratinator/agents/planner.md:87,98` — `"Report"` refers to this file's `## Report` (line 106) — OK
- `plugins/orchestratinator/agents/planner.md:96` — `"Write"` refers to this file's `## Write` (line 100) — OK
- "Tier rubric", "Sizing rules", "Validation checklist", "Build the Review Focus" — not referenced anywhere in quoted form; other files mention them only in lowercase prose ("the tier rubric", "the sizing rules", "the validation checklist" in `skills/plan/SKILL.md:97,129,140,181,183`, `skills/run/SKILL.md:129,136,229`, `agents/planner.md:47,82,94`, `agents/plan-reviewer.md:40,46`, `README.md:101`), each of which resolves to `reference/plan-format.md`'s `## Tier rubric` (line 270), `## Sizing rules` (line 285), or `## Validation checklist` (line 298) — OK
- `plugins/orchestratinator/README.md:101` — link `](reference/plan-format.md)` — target file exists at `plugins/orchestratinator/reference/plan-format.md` — OK
- `plugins/orchestratinator/reference/plan-format.md:225` — link `](#sequence-and-parallelism)` — target heading `## Sequence and parallelism` exists (line 252) — OK
- `plugins/orchestratinator/reference/plan-format.md:326` — link `](#format)` — target heading `### Format` exists (line 186) — OK

## Field references

- `- Batch:` — defined in `reference/plan-format.md:222` (Task fields table row), with the batch exception repeated at `:264` (Sequence and parallelism), `:290,291` (Sizing rules 2 and 3), and `:324` (Validation checklist) — OK
- `- Blocked:` — defined in `reference/plan-format.md:241` ("run appends these lines under a task") — OK
- `- Commit:` — defined in `reference/plan-format.md:230` (Task fields table row) — OK
- `- Consumes:` — defined in `reference/plan-format.md:234` (Interfaces row of Task fields table) — OK
- `- Escalated:` — defined in `reference/plan-format.md:240` ("run appends these lines under a task") — OK
- `- Fails first:` — defined in `reference/plan-format.md:229` (Task fields table row) — OK
- `- Format:` — defined in `reference/plan-format.md:188` (`### Format` section) — OK
- `- Origin:` — defined in `reference/plan-format.md:231` (Task fields table row) — OK
- `- Process:` — Change 0 text; `skills/run/SKILL.md:165` defines and writes it itself (D17), not `reference/plan-format.md` — OK
- `- Produces:` — defined in `reference/plan-format.md:234` (Interfaces row of Task fields table) — OK
- `- Status:` — defined in `reference/plan-format.md:77` (Header fields table, plan-level), `:83-85` (`### Milestone status`), and `:224` (Task fields table, task-level) — OK
- `- Tier adjustment:` — defined in `reference/plan-format.md:283` (Tier rubric note) — OK

No field name other than these 12 appears in `grep -rnoE '`- [A-Z][A-Za-z ]+:' plugins/orchestratinator` output.

## Agent references

- `orchestratinator:plan`, `orchestratinator:run`, `orchestratinator:status` — each is a directory under `plugins/orchestratinator/skills/` — OK
- `orchestratinator:planner`, `orchestratinator:reviewer`, `orchestratinator:scout`, `orchestratinator:scout-heavy`, `orchestratinator:plan-reviewer`, `orchestratinator:milestone-reviewer` — each is a file under `plugins/orchestratinator/agents/` (`planner.md`, `reviewer.md`, `scout.md`, `scout-heavy.md`, `plan-reviewer.md`, `milestone-reviewer.md`) — OK
- `orchestratinator:<tier>` (`skills/run/SKILL.md:158`) — the surrounding text ("Dispatch to the agent `orchestratinator:<tier>`") says the tier comes from the task's own Tier field, matching the allowed exception — OK
- `plugins/orchestratinator/agents/` contains exactly 10 files: `milestone-reviewer.md`, `plan-reviewer.md`, `planner.md`, `reviewer.md`, `scout-heavy.md`, `scout.md`, `specialist.md`, `worker-heavy.md`, `worker-light.md`, `worker.md`
- `plugins/orchestratinator/README.md:57-74` — "The cast" table has exactly 14 rows: 3 for the skills directories (`plan`, `run`, `status`, lines 61-63), 10 for the agent files (`planner` line 64, `scout` line 66, `scout-heavy` line 67, `reviewer` line 68, `plan-reviewer` line 69, `milestone-reviewer` line 70, `worker-light` line 71, `worker` line 72, `worker-heavy` line 73, `specialist` line 74), and 1 `Explore (built in)` row (line 65) — one row per directory in `skills/`, one row per file in `agents/`, plus Explore, no other row — OK

## Problems

1. `plugins/orchestratinator/skills/run/SKILL.md:256,267` — the parenthetical "(step 6 onward)" in both the **Retry** and **Block with GAP** sections doesn't resolve: `run/SKILL.md` has no `## 6.` section (its top-level numbering is only `## 1.` to `## 4.`), so there is no target named "step 6" anywhere in the file. The surrounding text ("finish the wave's other tasks first") most likely means `### 3e. Parallel wave`'s own item 6, "Integrate" (line 206), which is where a wave resumes after its other tasks' reports are in, but the reference as written doesn't name that or any other real target. (Step and item references; spec §16 read-through)
